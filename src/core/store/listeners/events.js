// Events used to trigger listeners
import { Enum } from '../../../utils';

const Events = new Enum([
    'VIEWS',
    'SEL_LABELS',
    'CSCALE',
    'MS_ELLIPSOIDS',
    'MS_LABELS',
    'EFG_ELLIPSOIDS',
    'EFG_LABELS'
]);

export default Events;